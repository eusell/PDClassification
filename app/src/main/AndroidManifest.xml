<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.majorProject.PDClassification">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MenuDrawer">
        <activity android:name="com.majorProject.PDClassification.Classifier" />
        <activity
            android:name="com.majorProject.PDClassification.MainActivity2"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.majorProject.PDClassification.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MenuDrawer.NoActionBar" />
    </application>

</manifest>